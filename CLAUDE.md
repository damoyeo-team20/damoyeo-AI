# 다모여 AI — 진행 중인 작업

## 지금 하고 있는 작업

다모여 AI(Back↔AI) API 명세서를 **프로토타입 화면 단위로** 확정하는 작업이다. 화면마다:

1. 사용자가 그 화면의 스크린샷을 준다.
2. 스크린샷에 실제로 표시되는 요소들을 보고, 그걸 채우려면 응답에 어떤 필드가 필요한지 판단한다. 화면에 없는 걸 미리 넣지 않는다 — 필요한 것만 정의한다.
3. 그 화면이 기존에 구현된 파이프라인/엔드포인트로 커버되는지, 새 엔드포인트가 필요한지 판단한다.
4. 요청/응답 필드와 간단한 에러 코드를 `docs/api-spec.md`에 화면별 절로 추가한다.
5. `backend-api-example.md`, `api-design.md`와 다른 점이 있으면 무엇이 다른지, 어느 쪽이 맞다고 보는지, 확인이 필요한지를 명시한다 — 조용히 하나로 합치지 않는다.

## 명세서는 어디에 쓰는가

**`docs/api-spec.md`** — 이번 작업의 결과물이 쌓이는 곳. 화면 단위로 절이 늘어난다. 화면 검토가 다 끝나면 이 내용을 `api-design.md`에 반영해서 하나로 합친다.

## 문서별 역할 (참고 우선순위)

| 문서 | 역할 |
| --- | --- |
| `docs/api-spec.md` | **이번 작업의 산출물.** 화면 단위로 확정한 요청/응답/에러코드. 가장 최신·구체적. |
| `docs/api-design.md` | 우리가 먼저 대충 설계해둔 API 초안(파이프라인 노드 단위). `api-spec.md`와 다르면 `api-spec.md`가 최신이다. |
| `docs/backend-api-example.md` | 백엔드가 임시로 작성해준 AI API 예시. **참고만 한다 — 그대로 따르지 않는다.** 백엔드가 어떤 형태를 기대하는지 가늠하는 용도. |
| `docs/ai-part-proposal.md` | AI 파트 전체 기획서. 파이프라인 구조·노드 역할·설계 근거의 배경 설명. |
| `docs/db_schema.md` | DB 스키마 확정본. 필드명·타입·enum 값의 최종 근거. |

## 판단 기준

- 화면에 보이는 텍스트/뱃지/버튼 각각이 어느 필드에서 오는지 명확히 짚는다. 애매하면 "확인 필요"로 남긴다.
- 새 필드를 추가할 땐 LLM이 만드는 값인지, 서버가 코드로 파생시키는 값인지 구분해서 적는다 (예: 태그 중 사실 판정은 서버가 붙이고 LLM 선택지에서 제외하는 식).
- 기존 코드(`app/schemas/`, `app/graph/nodes/`)와 다른 설계가 필요하면 문서에 먼저 반영하고, 실제 코드 변경은 사용자 확인 후 진행한다.
